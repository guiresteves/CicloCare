import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primárias
  static const Color primary = Color(0xFF3DBE8B);
  static const Color primaryDark = Color(0xFF2EA87A);
  static const Color primaryLight = Color(0xFFE8F8F2);

  // Fundo
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;

  // Texto
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFFADB5BD);

  // Input
  static const Color inputBackground = Color(0xFFF9FAFB);
  static const Color border = Color(0xFFE5E7EB);

  // Gradiente logo (verde → roxo)
  static const Color logoGreen = Color(0xFF3DBE8B);
  static const Color logoPurple = Color(0xFF9B59B6);

  // Outros
  static const Color error = Color(0xFFEF4444);
  static const Color divider = Color(0xFFEEEEEE);
  static const Color link = Color(0xFF3DBE8B);
}
